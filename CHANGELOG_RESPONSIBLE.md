# Changelog: Система назначения ответственных за задачи

## Дата: 2025-11-11

### ✨ Новый функционал

#### 1. Поддержка ответственных пользователей за задачи

- **Назначение при создании**: Можно указать ответственного при создании задачи через `responsibleId`
- **Автоматическое назначение**: Если не указан явно, автоматически назначается владелец API ключа
- **Изменение ответственного**: Можно изменить ответственного через PATCH /tasks/:id
- **Снятие ответственного**: Можно установить `responsibleId: null` для снятия ответственного
- **Валидация**: Ответственным можно назначить только участника доски (BoardMember)

### 🔧 Изменения в Backend

#### Обновленные файлы

1. **src/task/task.entity.ts**

   - Добавлено поле `responsible_id: string | null`
   - Добавлена связь `@ManyToOne(() => User) responsible`

2. **src/task/dto/task.dto.ts**

   - В `CreateTaskDto` добавлено `responsibleId?: string`
   - В `UpdateTaskDto` добавлено `responsibleId?: string`

3. **src/task/task.service.ts**

   - Метод `create()`: автоматическое назначение владельца API ключа + валидация
   - Метод `update()`: валидация что responsibleId является участником доски
   - Все методы получения: загружаются связи `responsible` и `apiKey.user`

4. **src/task/task.controller.ts**

   - Все эндпоинты возвращают преобразованные задачи через `transformTask()`

5. **src/task/task.transformer.ts** (новый файл)

   - `transformTask()`: преобразует задачу для безопасного ответа API
   - `transformTasks()`: пакетное преобразование
   - Автоматически определяет ответственного: `task.responsible ?? task.apiKey.user ?? null`
   - Убирает чувствительные данные пользователей (пароли и т.д.)

6. **src/task/task.module.ts**

   - Добавлен `ApiKey` в `TypeOrmModule.forFeature()`

7. **src/board/board.controller.ts**

   - Эндпоинты колонок возвращают задачи с преобразованием через `transformTasks()`

8. **src/board/board.service.ts**
   - Методы `getColumns()` и `getColumn()` загружают дополнительные связи:
     - `tasks.responsible`
     - `tasks.apiKey`
     - `tasks.apiKey.user`
     - `tasks.webhook`

### 📝 Новые файлы

1. **TASK_RESPONSIBLE_README.md**

   - Полная документация по работе с ответственными
   - Примеры API запросов
   - Описание логики

2. **FRONTEND_RESPONSIBLE_EXAMPLE.md**

   - Примеры React компонентов для работы с ответственными
   - Селектор участников доски
   - Форма создания/редактирования задачи
   - Карточка задачи с отображением ответственного

3. **migrations/add_responsible_to_tasks.sql**
   - SQL миграция для production
   - Добавление колонки `responsible_id`
   - Внешний ключ и индекс

### 🔄 Обновленные типы

#### Backend (`boards-tasks-types.ts`)

```typescript
export interface TaskResponsible {
  id: string;
  name: string;
  email: string;
}

export interface Task {
  // ... существующие поля
  responsible_id?: string | null;
  responsible?: TaskResponsible | null;
  responsibleId?: string | null;
}

export interface CreateTaskDto {
  // ... существующие поля
  responsible_id?: string | null;
}

export interface UpdateTaskDto {
  // ... существующие поля
  responsible_id?: string | null;
}
```

#### Frontend (`src/app/types/board.ts`)

```typescript
export interface TaskResponsible {
  id: string;
  name: string;
  email: string;
}

export interface Task {
  // ... существующие поля
  responsibleId?: string | null;
  responsible?: TaskResponsible | null;
}
```

### 🗄️ Изменения в БД

**Новая колонка в таблице `tasks`:**

- `responsible_id UUID NULL`
- Foreign Key к `users(id)` с ON DELETE SET NULL
- Индекс `idx_tasks_responsible_id`

**Для development:**

- Автоматически применится через `synchronize: true`

**Для production:**

- Запустить миграцию из `migrations/add_responsible_to_tasks.sql`

### 🔐 Безопасность

1. **Валидация при назначении**:

   - Проверяется что пользователь является участником доски
   - Возвращается 403 Forbidden если нет доступа

2. **Фильтрация данных пользователя**:

   - Из ответа API убираются пароли и другие чувствительные данные
   - Возвращается только: `id`, `name`, `email`

3. **Проверка доступа к доске**:
   - Используется `boardService.checkAccess()` для валидации

### 📊 API Endpoints

#### Получить участников доски

```
GET /boards/:id/members
Authorization: Bearer <token>

Response:
[
  {
    "id": "uuid",
    "user": {
      "id": "uuid",
      "name": "Имя",
      "email": "email@example.com"
    },
    "role": "owner" | "member"
  }
]
```

#### Создать задачу с ответственным

```
POST /tasks
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "Название",
  "boardId": "uuid",
  "responsibleId": "uuid" // опционально
}

Response:
{
  "id": "uuid",
  "title": "Название",
  "responsible": {
    "id": "uuid",
    "name": "Имя",
    "email": "email@example.com"
  },
  "responsibleId": "uuid"
}
```

#### Изменить ответственного

```
PATCH /tasks/:id
Authorization: Bearer <token>
Content-Type: application/json

{
  "responsibleId": "uuid" // или null
}
```

#### Получить задачи с ответственными

```
GET /tasks/by-board/:id
GET /tasks
GET /tasks/:id
Authorization: Bearer <token>

Response: Task[] | Task с полями responsible и responsibleId
```

### 🎯 Логика определения ответственного

**Приоритет в трансформере:**

1. Если есть `task.responsible` → используется он
2. Если нет, но есть `task.apiKey.user` → используется владелец ключа
3. Если ничего нет → `null`

**При создании задачи:**

1. Если `responsibleId` указан → проверка + назначение
2. Если не указан, но есть `apiKeyId` → автоматически владелец ключа
3. Если ничего нет → `null`

**При обновлении:**

1. Если `responsibleId` меняется → валидация что пользователь является участником доски
2. Можно установить `null` → ответственный снимается

### ⚠️ Breaking Changes

**Нет** - все изменения обратно совместимы:

- Новые поля опциональны
- Старые эндпоинты работают без изменений
- База данных: новая колонка nullable

### 🧪 Тестирование

Рекомендуется протестировать:

1. ✅ Создание задачи без responsibleId (автоназначение владельца ключа)
2. ✅ Создание задачи с responsibleId (участник доски)
3. ✅ Создание задачи с responsibleId (не участник доски) → 403
4. ✅ Обновление responsibleId (участник доски)
5. ✅ Обновление responsibleId (не участник доски) → 403
6. ✅ Обновление responsibleId на null (снятие ответственного)
7. ✅ Получение задачи - проверить поля responsible и responsibleId
8. ✅ Получение задач по доске - проверить вложенные задачи в колонках
9. ✅ Создание задачи через вебхук (responsible должен быть null)
10. ✅ Приглашение пользователя → назначение его ответственным

### 📚 Документация

- **TASK_RESPONSIBLE_README.md** - основная документация
- **FRONTEND_RESPONSIBLE_EXAMPLE.md** - примеры для фронтенда
- **migrations/add_responsible_to_tasks.sql** - SQL миграция

### 🚀 Развертывание

1. **Development:**

   ```bash
   # Автоматически применится через synchronize
   npm run start:dev
   ```

2. **Production:**

   ```bash
   # 1. Применить миграцию
   psql -U username -d database_name -f migrations/add_responsible_to_tasks.sql

   # 2. Перезапустить приложение
   npm run build
   npm run start:prod
   ```

### 🔄 Откат (Rollback)

Если нужно откатить изменения в production:

```sql
ALTER TABLE tasks DROP CONSTRAINT fk_tasks_responsible;
DROP INDEX idx_tasks_responsible_id;
ALTER TABLE tasks DROP COLUMN responsible_id;
```

Затем откатить код на предыдущую версию.

### 👥 Участники

- Backend: полная реализация
- Frontend: типы обновлены, примеры предоставлены
- Database: миграция подготовлена

---

**Версия**: 1.0.0  
**Статус**: ✅ Готово к использованию
