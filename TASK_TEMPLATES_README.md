# Шаблоны для задач

## Обзор

При создании задачи из входящего вебхука, данные автоматически форматируются через шаблон типа `TASK`. Это позволяет настроить, как заявки будут отображаться в виде задач на доске.

## Как это работает

### 1. Создание задачи из вебхука

Когда приходит вебхук:

1. Система ищет шаблон типа `TASK` для пользователя
2. Если шаблон найден - форматирует данные через него
3. Если шаблона нет - использует форматирование по умолчанию

### 2. Форматы шаблонов

#### Простой шаблон (без секций)

Используется только для описания задачи, заголовок генерируется автоматически.

```
Новая заявка с {{siteName}}
Форма: {{formName}}

Контактные данные:
Имя: {{data.name}}
Телефон: {{data.phone}}
Email: {{data.email}}
Сообщение: {{data.message}}
```

**Результат:**

- **Заголовок:** `Заявка от Иван Иванов` (автоматически)
- **Описание:** форматированный текст из шаблона

#### Шаблон с секциями

Позволяет настроить и заголовок, и описание.

```
[TITLE]
Заявка: {{data.name}} - {{data.phone}}

[DESCRIPTION]
**Контактная информация:**
- Имя: {{data.name}}
- Телефон: {{data.phone}}
- Email: {{data.email}}

**Сообщение:**
{{data.message}}

**Источник:** {{siteName}} - {{formName}}
**Дата:** {{createdAt}}
```

**Результат:**

- **Заголовок:** `Заявка: Иван Иванов - +7 999 123-45-67`
- **Описание:** форматированный текст из секции DESCRIPTION

## Доступные переменные

### Основные поля вебхука

- `{{siteName}}` - название сайта
- `{{formName}}` - название формы
- `{{createdAt}}` - дата и время создания

### Данные формы (через data.\*)

- `{{data.name}}` - имя
- `{{data.email}}` - email
- `{{data.phone}}` - телефон
- `{{data.message}}` - сообщение
- `{{data.любое_поле}}` - любое поле из формы

### Рекламные параметры (если есть)

- `{{advertisingParams.utm_source}}`
- `{{advertisingParams.utm_campaign}}`
- `{{advertisingParams.любой_utm}}`

### Метаданные

- `{{metadata.ip}}` - IP адрес
- `{{metadata.userAgent}}` - User Agent

## Примеры шаблонов

### Пример 1: Минималистичный

```
Имя: {{data.name}}
Телефон: {{data.phone}}
Email: {{data.email}}
```

### Пример 2: С форматированием Markdown

```
[TITLE]
📞 {{data.name}} - {{data.phone}}

[DESCRIPTION]
### Контактные данные

| Поле | Значение |
|------|----------|
| Имя | {{data.name}} |
| Телефон | {{data.phone}} |
| Email | {{data.email}} |

### Сообщение
{{data.message}}

---
*Источник:* {{siteName}} / {{formName}}
```

### Пример 3: С рекламными метками

```
[TITLE]
Заявка {{data.name}} | {{advertisingParams.utm_source}}

[DESCRIPTION]
**Клиент:**
- ФИО: {{data.name}}
- Телефон: {{data.phone}}
- Email: {{data.email}}

**Рекламные метки:**
- Источник: {{advertisingParams.utm_source}}
- Кампания: {{advertisingParams.utm_campaign}}
- Канал: {{advertisingParams.utm_medium}}

**Сообщение:**
{{data.message}}
```

### Пример 4: Для лендинга курсов

```
[TITLE]
Запись на курс: {{data.courseName}} - {{data.name}}

[DESCRIPTION]
🎓 **Запись на курс**

**Курс:** {{data.courseName}}
**Студент:** {{data.name}}
**Телефон:** {{data.phone}}
**Email:** {{data.email}}

**Комментарий:**
{{data.comment}}

**Предпочтительное время занятий:**
{{data.preferredTime}}

---
📅 Заявка получена: {{createdAt}}
🌐 Источник: {{siteName}}
```

### Пример 5: Для интернет-магазина

```
[TITLE]
Заказ: {{data.productName}} | {{data.name}}

[DESCRIPTION]
🛒 **Новый заказ**

**Товар:** {{data.productName}}
**Количество:** {{data.quantity}} шт.
**Цена:** {{data.price}} руб.

**Покупатель:**
- Имя: {{data.name}}
- Телефон: {{data.phone}}
- Email: {{data.email}}
- Адрес: {{data.address}}

**Способ оплаты:** {{data.paymentMethod}}
**Способ доставки:** {{data.deliveryMethod}}

**Комментарий к заказу:**
{{data.comment}}
```

## Создание и обновление шаблона

### Через API

```typescript
PUT /message-templates
Authorization: Bearer {token}
Content-Type: application/json

{
  "type": "task",
  "messageTemplate": "[TITLE]\nЗаявка от {{data.name}}\n\n[DESCRIPTION]\nТелефон: {{data.phone}}",
  "isEnabled": true
}
```

### Формат по умолчанию (если шаблона нет)

**Заголовок (автоматический):**

- Если есть `name` → `Заявка от {name}`
- Если есть `email` → `Заявка от {email}`
- Если есть `phone` → `Заявка от {phone}`
- Иначе → `Заявка с {siteName}`

**Описание (автоматическое):**

```
**Сайт:** example.com
**Форма:** Contact Form
**Дата:** 01.11.2025, 15:30:00

**Данные заявки:**
- **name:** Иван Иванов
- **phone:** +7 999 123-45-67
- **email:** ivan@example.com
- **message:** Интересует консультация
```

## Советы по созданию шаблонов

### 1. Используйте секции для контроля заголовка

Если хотите кастомный заголовок - используйте секции `[TITLE]` и `[DESCRIPTION]`:

```
[TITLE]
Ваш кастомный заголовок

[DESCRIPTION]
Ваше описание
```

### 2. Markdown поддерживается

Можно использовать Markdown для форматирования:

- `**жирный текст**`
- `*курсив*`
- `### Заголовки`
- `- списки`
- `[ссылки](url)`

### 3. Проверяйте наличие полей

Используйте только те поля, которые точно будут в ваших формах:

- `{{data.name}}` - если в форме есть поле name
- `{{data.phone}}` - если в форме есть поле phone

Если поля нет - будет пустая строка.

### 4. Тестируйте шаблон

Отправьте тестовый вебхук и проверьте как выглядит задача.

## Примеры использования

### Настройка шаблона для лендинга

```javascript
// 1. Создаем API ключ (автоматически создается доска)
const apiKey = await fetch('/api-keys', {
  method: 'POST',
  body: JSON.stringify({ name: 'Лендинг курсов' }),
});

// 2. Создаем шаблон для задач
await fetch('/message-templates', {
  method: 'PUT',
  body: JSON.stringify({
    type: 'task',
    messageTemplate: `
[TITLE]
Запись: {{data.name}} на {{data.courseName}}

[DESCRIPTION]
**Студент:** {{data.name}}
**Телефон:** {{data.phone}}
**Email:** {{data.email}}
**Курс:** {{data.courseName}}
    `.trim(),
    isEnabled: true,
  }),
});

// 3. Теперь все заявки будут форматироваться по этому шаблону
```

### Получение текущего шаблона

```javascript
const response = await fetch('/message-templates', {
  headers: { Authorization: `Bearer ${token}` },
});

const { userTemplates } = await response.json();
const taskTemplate = userTemplates.find((t) => t.type === 'task');

if (taskTemplate) {
  console.log('Текущий шаблон:', taskTemplate.messageTemplate);
} else {
  console.log('Шаблон не настроен, используется формат по умолчанию');
}
```

## Обработка ошибок

### Если переменная не найдена

```
Шаблон: {{data.missingField}}
Результат: {{data.missingField}} (placeholder остается как есть)
```

### Если значение null/undefined

```
Шаблон: {{data.emptyField}}
Результат: (пустая строка)
```

### Если значение - объект

```
Шаблон: {{data.complexObject}}
Результат: (пустая строка)
```

## FAQ

### Можно ли использовать условную логику в шаблоне?

Нет, шаблоны поддерживают только простую подстановку переменных. Для сложной логики создайте несколько шаблонов или обрабатывайте на фронтенде.

### Как использовать вложенные поля?

Используйте точку для доступа к вложенным полям:

```
{{data.user.name}}
{{advertisingParams.utm.source}}
```

### Можно ли изменить шаблон после создания?

Да, просто отправьте PUT запрос с новым шаблоном. Все новые задачи будут использовать новый шаблон.

### Применяется ли шаблон к существующим задачам?

Нет, шаблон применяется только при создании новых задач. Существующие задачи не изменяются.

### Нужно ли создавать шаблон для каждой доски?

Нет, шаблон один для всего пользователя. Все доски используют один шаблон типа TASK.

## Интеграция с фронтендом

### Редактор шаблона

```tsx
function TaskTemplateEditor() {
  const [template, setTemplate] = useState('');

  const variables = [
    { key: 'siteName', desc: 'Название сайта' },
    { key: 'formName', desc: 'Название формы' },
    { key: 'data.name', desc: 'Имя из формы' },
    { key: 'data.phone', desc: 'Телефон' },
    { key: 'data.email', desc: 'Email' },
  ];

  const insertVariable = (key) => {
    setTemplate((prev) => prev + `{{${key}}}`);
  };

  const saveTemplate = async () => {
    await fetch('/message-templates', {
      method: 'PUT',
      body: JSON.stringify({
        type: 'task',
        messageTemplate: template,
        isEnabled: true,
      }),
    });
  };

  return (
    <div>
      <h3>Редактор шаблона задач</h3>

      <div>
        <h4>Доступные переменные:</h4>
        {variables.map((v) => (
          <button onClick={() => insertVariable(v.key)}>
            {v.key} - {v.desc}
          </button>
        ))}
      </div>

      <textarea
        value={template}
        onChange={(e) => setTemplate(e.target.value)}
        placeholder="Введите шаблон..."
      />

      <button onClick={saveTemplate}>Сохранить</button>
    </div>
  );
}
```
